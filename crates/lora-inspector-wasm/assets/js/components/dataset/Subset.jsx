import React from "react";

// Placeholder for Subset component - will be fully implemented later
export function Subset({ subset, metadata }) {
	return (
		<div className="subset">
			<h4>{subset.image_dir}</h4>
			{/* TODO: Implement full Subset component */}
		</div>
	);
}
